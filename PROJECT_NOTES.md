# Project Notes

## Approved Direction

- Prefer WHO modeled incidence as the training target when the raw file includes `e_inc_100k` or `e_inc_num`.
- Keep a clear fallback to the notification-derived proxy when modeled incidence is absent, and surface that source honestly in the UI.
- Add a Prioritization tab that ranks countries by estimated opportunity under a chosen scenario.

## Future Upgrade

- If the project needs true Dribbble-level polish beyond Streamlit's limits, rebuild the frontend as a dedicated React app with a real design system.
